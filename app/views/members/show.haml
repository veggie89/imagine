#user_info
	.avatar
		= image_tag(@user.avatar)
	.info
		.name
			= @user.name
		.providers
			- @user.authorizations.each do |p|
				= link_to image_tag("icon/#{p.provider}_big.png"),p.link,:target => "blank"

- content_for :footer do
	#init.hide(data-js="UserShow")